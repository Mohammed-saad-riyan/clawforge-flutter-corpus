# App Template Library

This directory stores high-level app templates used by ClawForge for rule-based template matching.

Each template is a structured YAML file containing:
- name
- keywords
- architecture
- state management
- screens
- models
- services
- packages

Templates should cover the most common app request classes and be designed for deterministic matching before any LLM generation.
